include (
  val NiceComponents.code(
        ~debugName="Code",
        [
          Raw("background-color", "#f8faff"),
          Display(Block),
          Raw("padding", "1rem"),
          BorderRadius(Px(3))
        ]
      )
);
