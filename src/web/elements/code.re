include (
  val NiceComponents.code(
        ~debugName="Code",
        [|
          BackgroundColor(Hex("f8faff")),
          Display(Block),
          Padding(Rem(1.)),
          BorderRadius(Px(3))
        |]
      )
);
