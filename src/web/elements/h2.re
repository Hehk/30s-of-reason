include (
  val NiceComponents.h2(
        ~debugName="H2",
        [|
          Raw("font-size", "1.5rem"),
          Margin(Px(0)),
          MarginBottom(Rem(1.)),
          MarginTop(Rem(2.)),
          Color(Hex("db4d3f"))
        |]
      )
);
