include (
  val NiceComponents.div(
        ~debugName="Background",
        [Raw("min-height", "100vh"), FontFamily("Karla, sans-serif"), Raw("background-color", "#f8faff"),
        Height(Percent(100.))
        ]
      )
);
